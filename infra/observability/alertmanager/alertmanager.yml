global:
  resolve_timeout: 5m

route:
  receiver: helix-null
  group_by: ["alertname", "severity", "priority", "service", "slo", "tier", "plan_id", "region"]
  group_wait: 10s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: helix-null
