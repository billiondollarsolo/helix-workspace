{{- if .Values.monitoring.prometheusRule.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: {{ include "helix.fullname" . }}-product-slos
  {{- with .Values.monitoring.prometheusRule.namespace }}
  namespace: {{ . | quote }}
  {{- end }}
  labels:
    {{- include "helix.labels" . | nindent 4 }}
    helix.io/alert-domain: product-slos
    {{- with .Values.monitoring.prometheusRule.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with .Values.monitoring.prometheusRule.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
{{- $rules := .Files.Get "files/helix-product-slos.yml" | fromYaml }}
{{- toYaml $rules | nindent 2 }}
{{- end }}
