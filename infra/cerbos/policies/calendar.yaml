apiVersion: api.cerbos.dev/v1
resourcePolicy:
  resource: calendar
  version: default
  importDerivedRoles:
    - helix_platform_roles
  schemas:
    principalSchema:
      ref: cerbos:///platform/principal.json
    resourceSchema:
      ref: cerbos:///platform/resource.json
  rules:
    - actions: ["*"]
      effect: EFFECT_ALLOW
      roles: ["admin"]
      condition:
        match:
          expr: P.attr.org_id == R.attr.org_id

    - actions: ["create", "list", "search"]
      effect: EFFECT_ALLOW
      derivedRoles: ["org_member"]

    - actions: ["read", "list_events", "search"]
      effect: EFFECT_ALLOW
      derivedRoles: ["shared"]

    - actions: ["read", "list_events", "create_event", "update_event", "search"]
      effect: EFFECT_ALLOW
      derivedRoles: ["participant"]

    - actions: ["read", "list_events", "search"]
      effect: EFFECT_ALLOW
      derivedRoles: ["org_member"]
      condition:
        match:
          expr: R.attr.visibility == "org"

    - actions: ["*"]
      effect: EFFECT_ALLOW
      derivedRoles: ["owner"]
