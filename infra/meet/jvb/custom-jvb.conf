# The upstream container defaults to trusting every XMPP certificate. The
# private CA truststore mounted by Compose is the only accepted issuer.
videobridge.apis.xmpp-client.configs.shard0.DISABLE_CERTIFICATE_VERIFICATION = false

# Never resolve attacker-controlled ICE candidate hostnames in the media plane.
videobridge.ice.resolve-remote-candidates = false
